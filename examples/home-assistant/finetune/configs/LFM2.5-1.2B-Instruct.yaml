project_name: "home-assistant-1.2B"
model_name: "LFM2.5-1.2B-Instruct"
training_type: "sft"

dataset:
  path: "Paulescu/home-assistant-sft"
  type: "sft"

training_config:
  extends: "DEFAULT_SFT"
  num_train_epochs: 3
  per_device_train_batch_size: 2
  learning_rate: 2e-4
  use_liger_kernel: true
  tracker: "trackio"
  trackio_space_id: "Paulescu/home-assistant-finetune"

peft_config:
  extends: "DEFAULT_LORA"
  use_peft: true

modal:
  app_name: "leap-finetune"
  gpu: "H100:1"
  timeout: 7200
  output_volume: "leap-finetune"
  output_dir: "/outputs"
  detach: false
